<bases:UserControlBase
    x:Class="BlackGoldAncientSword.Modules.UI.Search.Views.SearchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:bases="clr-namespace:BlackGoldAncientSword.Framework.Core.Bases.Views;assembly=BlackGoldAncientSword.Framework"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:viewmodels="clr-namespace:BlackGoldAncientSword.Modules.UI.Search.ViewModels"
    d:DesignHeight="600"
    d:DesignWidth="800"
    Background="WhiteSmoke"
    FontFamily="Segoe UI"
    mc:Ignorable="d">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,16"
            FontSize="22"
            FontWeight="Bold"
            Text="{DynamicResource Search.Title}" />

        <Border
            Grid.Row="1"
            Margin="0,0,0,16"
            Padding="8"
            Background="White"
            BorderBrush="LightGray"
            BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0">
                    <TextBox
                        Name="SearchTextBox"
                        Height="32"
                        VerticalAlignment="Center"
                        Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock
                        Margin="2,0,0,0"
                        VerticalAlignment="Center"
                        Foreground="Gray"
                        IsHitTestVisible="False"
                        Text="{DynamicResource Search.Input}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text, ElementName=SearchTextBox}" Value="">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Text, ElementName=SearchTextBox}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
                <Button
                    Grid.Column="1"
                    MinWidth="80"
                    Height="32"
                    Margin="8,0,0,0"
                    ToolTip="{DynamicResource Search.Button}" Content="{DynamicResource Search.Button}">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <i:InvokeCommandAction Command="{Binding SearchCommand}" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>
            </Grid>
        </Border>

        <!--  Search history  -->
        <Border
            Grid.Row="2"
            Background="White"
            BorderBrush="LightGray"
            BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock
                    Grid.Row="0"
                    Margin="12,8,12,4"
                    FontSize="14"
                    FontWeight="Bold"
                    Text="{DynamicResource Search.History}" />
                <ListBox
                    Grid.Row="1"
                    Margin="4"
                    HorizontalContentAlignment="Stretch"
                    d:ItemsSource="{d:SampleData ItemCount=5}"
                    BorderThickness="0"
                    ItemsSource="{Binding SearchHistory}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="4,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock
                                    Grid.Column="0"
                                    VerticalAlignment="Center"
                                    d:Text="PlayerName"
                                    Text="{Binding Query}" />
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="8,0,8,0"
                                    VerticalAlignment="Center"
                                    d:Text="2026-01-01 12:00"
                                    FontSize="11"
                                    Foreground="Gray"
                                    Text="{Binding Timestamp, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" />
                                <Button
                                    Grid.Column="2"
                                    Padding="4,0"
                                    VerticalAlignment="Center"
                                    Background="Transparent"
                                    BorderBrush="LightGray"
                                    BorderThickness="1"
                                    CommandParameter="{Binding}" ToolTip="{DynamicResource Search.Copy}" Content="{DynamicResource Search.Copy}">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding DataContext.CopyCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}" />
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Button>
                                    FontSize="11"
                                    Foreground="Gray" />
                                <Button
                                    Grid.Column="3"
                                    Margin="4,0,0,0"
                                    Padding="4,0"
                                    VerticalAlignment="Center"
                                    Background="Transparent"
                                    BorderBrush="LightGray"
                                    BorderThickness="1"
                                    CommandParameter="{Binding}" ToolTip="{DynamicResource Search.Delete}" Content="{DynamicResource Search.Delete}">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="Click">
                                            <i:InvokeCommandAction Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}" />
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Button>
                                    FontSize="11"
                                    Foreground="Gray" />
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</bases:UserControlBase>
