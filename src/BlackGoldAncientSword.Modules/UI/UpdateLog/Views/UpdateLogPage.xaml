<bases:UserControlBase
    x:Class="BlackGoldAncientSword.Modules.UI.UpdateLog.Views.UpdateLogPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:bases="clr-namespace:BlackGoldAncientSword.Framework.Core.Bases.Views;assembly=BlackGoldAncientSword.Framework"
    xmlns:consts="clr-namespace:BlackGoldAncientSword.Framework.Core.Consts;assembly=BlackGoldAncientSword.Framework"
    xmlns:ctrls="clr-namespace:BlackGoldAncientSword.Framework.UI.Controls;assembly=BlackGoldAncientSword.Framework"
    xmlns:ext="clr-namespace:BlackGoldAncientSword.Framework.Core.Extensions;assembly=BlackGoldAncientSword.Framework"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="clr-namespace:HandyControl.Controls;assembly=HandyControl"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="Transparent"
    mc:Ignorable="d">
    <bases:UserControlBase.Resources>
        <ext:MultiplyConverter x:Key="MultiplyConverter" />
    </bases:UserControlBase.Resources>
    <ctrls:OverlayHost
        CardMaxHeight="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource MultiplyConverter}, ConverterParameter=0.8}"
        CardMaxWidth="1080"
        CardMinWidth="450"
        CardPadding="10"
        RegionName="{x:Static consts:GlobalConstant.UpdateLogRegion}"
        Title="{DynamicResource Announcement.UpdateHistory}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border
                Grid.Row="0"
                Margin="0,0,0,16"
                Background="{DynamicResource SurfaceSecondaryBrush}"
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <hc:CircleProgressBar
                        Width="56"
                        Height="56"
                        ArcThickness="4"
                        Foreground="{DynamicResource AccentPrimaryBrush}"
                        IsIndeterminate="True" />
                    <TextBlock
                        Margin="0,16,0,0"
                        HorizontalAlignment="Center"
                        FontSize="{DynamicResource FontSize.Metadata}"
                        Foreground="{DynamicResource TextPrimaryBrush}"
                        Text="{DynamicResource Announcement.Loading}" />
                </StackPanel>
            </Border>

            <ScrollViewer
                Grid.Row="0"
                Margin="0,0,0,16"
                Visibility="{Binding HasData, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel>
                    <ItemsControl ItemsSource="{Binding UpdateHistory}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Border
                                        Grid.Column="0"
                                        Padding="4,1"
                                        VerticalAlignment="Top"
                                        Background="{DynamicResource AccentLightBrush}"
                                        CornerRadius="3">
                                        <TextBlock
                                            FontSize="{DynamicResource FontSize.Caption}"
                                            Foreground="{DynamicResource AccentPrimaryBrush}"
                                            Text="{Binding Version}" />
                                    </Border>
                                    <TextBlock
                                        Grid.Column="1"
                                        Margin="10,0,0,0"
                                        FontSize="{DynamicResource FontSize.Metadata}"
                                        Foreground="{DynamicResource TextPrimaryBrush}"
                                        Text="{Binding Detail}"
                                        TextWrapping="Wrap" />
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>

            <StackPanel
                Grid.Row="1"
                HorizontalAlignment="Right"
                Orientation="Horizontal">
                <Button
                    MinWidth="80"
                    Height="32"
                    Command="{Binding ConfirmCommand}"
                    Content="{DynamicResource Announcement.Confirm}"
                    ToolTip="{DynamicResource Announcement.Confirm}" />
            </StackPanel>
        </Grid>
    </ctrls:OverlayHost>
</bases:UserControlBase>
