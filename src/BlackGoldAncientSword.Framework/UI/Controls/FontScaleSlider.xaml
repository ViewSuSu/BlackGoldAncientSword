<UserControl
    x:Class="BlackGoldAncientSword.Framework.UI.Controls.FontScaleSlider"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="Root"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,6"
            FontSize="{DynamicResource FontSize.Body}"
            FontWeight="SemiBold"
            Foreground="{DynamicResource TextPrimaryBrush}"
            Text="{DynamicResource Settings.FontSize.Title}" />

        <!-- 标准 / 更大 标签 -->
        <Grid Grid.Row="1" Margin="8,0,8,2">
            <TextBlock
                HorizontalAlignment="Left"
                FontSize="{DynamicResource FontSize.Metadata}"
                Foreground="{DynamicResource TextMutedBrush}"
                Text="{DynamicResource Settings.FontSize.Standard}" />
            <TextBlock
                HorizontalAlignment="Right"
                FontSize="{DynamicResource FontSize.Metadata}"
                Foreground="{DynamicResource TextMutedBrush}"
                Text="{DynamicResource Settings.FontSize.Large}" />
        </Grid>

        <!-- 滑块 -->
        <Slider
            Grid.Row="2"
            Minimum="0"
            Maximum="5"
            TickFrequency="1"
            IsSnapToTickEnabled="True"
            TickPlacement="BottomRight"
            AutoToolTipPlacement="TopLeft"
            Value="{Binding FontScale, ElementName=Root}" />

        <!-- 刻度数字（6 档，均匀分布，左右缩进对齐滑块轨道起止点） -->
        <Grid Grid.Row="3" Margin="8,2,8,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" HorizontalAlignment="Left" FontSize="{DynamicResource FontSize.Caption}" Foreground="{DynamicResource TextMutedBrush}" Text="13" />
            <TextBlock Grid.Column="1" HorizontalAlignment="Center" FontSize="{DynamicResource FontSize.Caption}" Foreground="{DynamicResource TextMutedBrush}" Text="14" />
            <TextBlock Grid.Column="2" HorizontalAlignment="Center" FontSize="{DynamicResource FontSize.Caption}" Foreground="{DynamicResource TextMutedBrush}" Text="15" />
            <TextBlock Grid.Column="3" HorizontalAlignment="Center" FontSize="{DynamicResource FontSize.Caption}" Foreground="{DynamicResource TextMutedBrush}" Text="16" />
            <TextBlock Grid.Column="4" HorizontalAlignment="Center" FontSize="{DynamicResource FontSize.Caption}" Foreground="{DynamicResource TextMutedBrush}" Text="17" />
            <TextBlock Grid.Column="5" HorizontalAlignment="Right" FontSize="{DynamicResource FontSize.Caption}" Foreground="{DynamicResource TextMutedBrush}" Text="18" />
        </Grid>
    </Grid>
</UserControl>
